CLI tool and Go library that provides (security) information on IP addresses.
It runs various checks to get the information. Most checks are passive, i.e.
not interacting directly with the IP address. Active checks, like ping and
tls, are not aggressive and can be disabled with -no-active.
