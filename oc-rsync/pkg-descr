rsync re-implemented in Rust. Wire-compatible with upstream rsync 3.5.0 (and
with the whole 3.4.x series, protocol 32), works as a drop-in replacement.

Binary name: oc-rsync - installs alongside system rsync without conflict.
